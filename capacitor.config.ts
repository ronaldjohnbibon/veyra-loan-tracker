import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.veyra.loantracker',
  appName: 'Veyra Loan Tracker',
  webDir: 'dist',
};

export default config;
